<p id="notice"><%= notice %></p>

<p>
  <strong><%= Task.human_attribute_name(:title) %></strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:description) %></strong>
  <%= @task.description %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:deadline) %></strong>
  <%= @task.deadline %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:priority) %></strong>
  <%= @task.priority %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:status) %></strong>
  <%= @task.status %>
</p>

<%= link_to t('label.edit'), edit_task_path(@task) %> |
<%= link_to t('label.back'), tasks_path %>
